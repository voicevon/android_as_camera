# Android 网络摄像机 技术设计文档

## 1. 技术栈

| 项 | 选型 |
|------|------|
| 语言 | Kotlin |
| 相机 | Camera2 API（YUV_420_888 输出） |
| 编码 | MediaCodec（H.264） |
| RTSP | 自研轻量 Server（TCP 交错 RTP，监听 8554） |
| MQTT | Eclipse Paho（mqttv3） |
| UI | 单 Activity + ViewModel（MVVM） |
| 系统要求 | minSdk 26，targetSdk 34 |

## 2. 总体架构

```
                    MQTT Broker (voicevon.vicp.io:1883)
                        ↑ cmd / state / event
                        │
┌─────────────── 摄像机端 App（前台服务） ───────────────┐
│                                                        │
│  Camera2 ──YUV帧──┬─→ H264Encoder ──H.264码流──┬─→ RtspServer → VLC / 观看端App
│                   │                            └─→ RecordingManager（录像时）
│                   ├─→ MotionDetector ──移动事件──→ MQTT event
│                   └─→ PhotoTaker（拍照时，YUV→JPEG）
│                                                        │
│  MqttControl ←── 远程命令 ──→ 各模块控制接口            │
│  MainActivity（预览 + 本地控制按钮）                    │
└────────────────────────────────────────────────────────┘
```

## 3. 关键设计决策

| # | 决策 | 理由 |
|---|------|------|
| D1 | 码流复用：一路 H.264 编码输出同时供 RTSP 推流和录像 MP4 复用 | 只有一个编码器，避免多 surface 会话兼容性问题 |
| D2 | RTP 走 TCP 交错传输（interleaved） | 无需 UDP 端口协商，VLC 开箱即用 |
| D3 | 拍照复用 YUV 帧 → JPEG | 避免同时绑定过多相机用例（如需高清可后续加 ImageCapture） |
| D4 | 推流跑在 `foregroundServiceType="camera"` 的前台服务 + WakeLock | 保证息屏、后台推流不中断 |
| D5 | Android 工程放在仓库子目录 `CameraApp/` | 满足根目录文件数限制，文档与代码分离 |
| D6 | 观看端 App 为独立工程，不在本仓库 | 视频仅局域网；观看端另行立项 |

## 4. 类设计（申请批准）

新增类申请如下，共 11 个：

| 类名 | 功能与作用 |
|------|-----------|
| `CameraStreamService` | 前台服务，承载推流全生命周期；App 退后台/息屏时保持相机与编码运行；持有各模块并处理销毁回收 |
| `CameraController` | 封装 Camera2：打开/关闭相机、前后摄切换、变焦、预览 Surface 输出；将 YUV 帧分发给编码、移动侦测、拍照三个消费方 |
| `H264Encoder` | 封装 MediaCodec：YUV 输入 → H.264 NAL 单元输出；维护 SPS/PPS 供 RTSP SDP 与录像使用 |
| `RtspServer` | 监听 8554 端口，处理 RTSP 信令（OPTIONS/DESCRIBE/SETUP/PLAY/TEARDOWN），生成 SDP，管理客户端会话的建立与销毁 |
| `RtspSession` | 单个客户端会话：H.264 码流按 RFC 6184 打包（单 NAL / FU-A），通过 TCP 交错方式发送 RTP |
| `MqttControl` | 封装 Paho：连接/自动重连 Broker；解析 cmd 命令并分发回调；对外提供 state/event 发布方法 |
| `MotionDetector` | 对 YUV 帧做分块亮度差分；含灵敏度参数与冷却防抖；输出移动事件 |
| `RecordingManager` | 录像控制：接收 H.264 码流与 MediaMuxer 合成 mp4 保存；上报开始/停止与文件路径 |
| `PhotoTaker` | 拍照：取最新 YUV 帧转 JPEG，保存到图库，回报文件路径 |
| `StatusReporter` | 周期采集并上报状态：在线、推流状态、当前摄像头、局域网 IP、电量 |
| `StreamConfig` | 配置存取：分辨率、端口、灵敏度、MQTT 参数；SharedPreferences 实现，供 UI 与各模块读取 |

UI 层不单独申请类：`MainActivity` + `CameraViewModel`（预览、本地控制、状态展示）属 Android 模板必需结构。

## 5. 关键流程

### 5.1 启动推流
1. 用户点击开始（或收到 MQTT start_stream 命令）
2. 启动前台服务 → 打开相机 → 启动编码器 → 启动 RTSP Server
3. VLC 连接后：DESCRIBE 返回 SDP（含 SPS/PPS）→ SETUP/PLAY → 会话开始收码流

### 5.2 拍照 / 录像（本地或远程触发）
- 拍照：取最新 YUV 帧 → JPEG → 存图库 → MQTT event 上报路径
- 录像：开始接收编码码流 → MediaMuxer 写 mp4 → 停止后 MQTT event 上报路径

### 5.3 移动侦测
- 编码取帧的同时抽样送入 MotionDetector → 判定移动 → MQTT event 告警（冷却期内抑制）

### 5.4 远程命令分发
- MqttControl 收到 cmd → 查命令表 → 调对应模块接口 → 执行结果写回 state/event

## 6. MQTT 接口定义

主题沿用需求文档：`camera/{设备ID}/cmd`、`camera/{设备ID}/state`、`camera/{设备ID}/event`

命令（cmd）：

| action | 参数 | 说明 |
|--------|------|------|
| start_stream / stop_stream | - | 开关推流 |
| switch_camera | front / back | 切换摄像头 |
| zoom | ratio 数值 | 变焦 |
| capture | - | 拍照 |
| start_record / stop_record | - | 开关录像 |
| motion | enabled, sensitivity | 移动侦测开关与灵敏度 |

状态（state，周期上报）：streaming、camera、resolution、ip、battery

事件（event）：type=motion / photo / record（附文件路径或灵敏度信息）

## 7. 工程结构

```
android_network_camera/
├── 需求文档.md
├── 技术设计文档.md
└── CameraApp/          # Android 工程（Gradle）
    └── app/src/main/java/.../（按第4节类组织）
```
